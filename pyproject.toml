[project]
name = "gymboy"
version = "0.0.1"
description = "Gameboy (Color) environments in Gymnasium 🤖"
authors = [{ name = "Dennis Jabs" }]
requires-python = ">=3.10"
readme = "README.md"
license = { file = "LICENSE" }

dependencies = ["gymnasium>=1.1.1", "pyboy>=2.5.3", "scikit-image>=0.25.2"]

[dependency-groups]
dev = [
    "bandit>=1.9.4",
    "deptry>=0.25.1",
    "ipykernel>=7.3.0",
    "pytest>=7.4.0",
    "pytest-cov>=4.1.0",
    "pytest-mock>=3.14.1",
    "ruff>=0.12.1",
    "rumdl>=0.2.18",
    "tox-uv>=1.35.2",
    "ty>=0.0.50",
]

[project.urls]
Homepage = "https://github.com/nobodyPerfecZ/gymboy"
Issues = "https://github.com/nobodyPerfecZ/gymboy/issues"

[[tool.uv.index]]
name = "testpypi"
url = "https://test.pypi.org/simple/"
publish-url = "https://test.pypi.org/legacy/"
explicit = true

[tool.ruff]
line-length = 88
lint.ignore = ["E203"]
lint.select = ["E", "F", "B", "I"]
lint.mccabe.max-complexity = 10
target-version = "py310"
